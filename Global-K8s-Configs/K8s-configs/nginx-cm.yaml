apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-proxy-conf
  namespace: fampay-dev
data:
  default.conf: |
    server {
      listen 80;

      location /hodr/ {
        proxy_pass http://hodr.fampay-dev.svc.cluster.local:8080/;
      }
      location /bran/ {
        proxy_pass http://bran.fampay-dev.svc.cluster.local:8000/;
      }
    }
